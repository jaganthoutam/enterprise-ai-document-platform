{
  "name": "auth-service",
  "version": "1.0.0",
  "description": "Authentication service for BedrockAI",
  "main": "auth-lambda.js",
  "scripts": {
    "start": "node auth-lambda.js",
    "test": "jest",
    "build": "tsc --build tsconfig.json",
    "lint": "eslint . --ext .js,.ts",
    "format": "prettier --write \"**/*.{js,ts,json}\""
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0",
    "jsonwebtoken": "^9.0.0",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "@types/aws-lambda": "^8.10.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
} 